<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quiz</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="container">
        <h1>Quiz Time!</h1>
        <form action="/submit" method="POST">
            <div class="form-group">
                <label for="username">Enter Your Name:</label>
                <input type="text" id="username" name="username" required>
            </div>
            <hr>
            <% if (locals.questions && questions.length > 0) { %>
                <% questions.forEach((q, index) => { %>
                    <div class="question-block">
                        <h3><%= index + 1 %>. <%= q.question %></h3>
                        <% if (q.options) { %>
                            <% q.options.forEach(option => { %>
                                <div class="option">
                                    <%# This is the fix. (option || '') makes sure we never call .replace() on null %>
                                    <input type="radio" id="q<%= index %>-<%= (option || '').replace(/\s/g, '-') %>" name="answers[<%= index %>]" value="<%= option %>" required>
                                    <label for="q<%= index %>-<%= (option || '').replace(/\s/g, '-') %>"><%= option || '[Blank Option]' %></label>
                                </div>
                            <% }); %>
                        <% } %>
                    </div>
                <% }); %>
                <button type="submit" class="btn">Submit Quiz</button>
            <% } else { %>
                <p>No questions available to display.</p>
            <% } %>
        </form>
    </div>
</body>
</html>